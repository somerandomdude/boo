<nav class="pagination" role="navigation">
  {{#if prev}}
    <a class="button newer-posts" href="{{page_url prev}}">Newer Posts</a>
  {{/if}}
  {{#if next}}
    <a class="button older-posts" href="{{page_url next}}">Older Posts</a>
  {{/if}}
</nav>
